import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'dark', // inside the palette we are enabling dark mode, // mode: 'light', - light mode
  },
});